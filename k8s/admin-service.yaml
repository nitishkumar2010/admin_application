# apiVersion: v1
# kind: Service
# metadata:
#   name: admin
# spec:
#   type: NodePort
#   selector:
#     app: admin
#   ports:
#     - port: 3000
#       targetPort: 3000
#       nodePort: 30007

apiVersion: v1
kind: Service
metadata:
  name: admin
  namespace: healthvault
spec:
  type: ClusterIP
  selector:
    app: admin
  ports:
    - name: http
      port: 80
      targetPort: 3000
